package One;

public class Tester {
    public static void main(String[] args) {
        PrivateModifier privatemodifier = new PrivateModifier();
        privatemodifier.printInfo();
    }
}
/* the variable could be accessed since it's in the class itself */